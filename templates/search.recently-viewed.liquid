{% layout none %}
{% paginate search.results by 50 %}
  <div class="grid product-grid">
    {% for product in search.results %}
      <li class="grid__item" data-handle="{{ product.handle }}">
        {% render 'card-product',
          card_product: product,
          media_aspect_ratio: settings.wishlist_image_ratio,
          show_secondary_image: true,
          show_vendor: settings.wishlist_show_vendor,
          show_rating: settings.wishlist_show_rating
        %}
      </li>
    {% endfor %}
  </div>
{% endpaginate %}
